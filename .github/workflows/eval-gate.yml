name: Eval gate

on: pull_request

permissions:
  contents: read
  pull-requests: write

jobs:
  gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Gate the example eval results
        uses: ./
        with:
          baseline: examples/baseline.jsonl
          candidate: examples/candidate.jsonl
